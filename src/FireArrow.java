public class FireArrow extends HeroWeapon{
    /**
     * The `FireArrow` class represents a specific hero weapon: a fire arrow.
     * This class inherits from the `HeroWeapon` class.
     */
    public FireArrow() {
        /**
         * Default constructor for the `FireArrow` class.
         * Initializes the fire arrow's attack points (25) and attack bonus (15).
         */
        super(25,15);

    }
}
