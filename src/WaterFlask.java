public class WaterFlask extends HeroWeapon {
    /**
     * The `WaterFlask` class represents a specific hero weapon: a water flask.
     * This class inherits from the `HeroWeapon` class.
     */
    public WaterFlask() {
        /**
         * Default constructor for the `WaterFlask` class.
         * Initializes the water flask's attack points (10) and attack bonus (6).
         */
        super(10,6);

    }
}
